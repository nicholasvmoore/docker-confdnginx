# Generated by confd {{datetime}}

upstream php {
  {{ range gets "/services/dev/phpfpm/*" }}
  {{ $server := split (.Value) "," }}
  server {{index $server 0}}:{{index $server 1}};
  {{end}}
}

server {
  server_name _;

  fastcgi_pass php;

}
